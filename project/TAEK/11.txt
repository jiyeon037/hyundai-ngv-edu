git config --global user.name HYOTAEKCHOI
git config --global user.email taekk19962 // 매번 물어보는 귀찮음을 피하기 위해 설정.

git clone https://github.com/jiyeon037/project

: 깃허브의 레포지토리의 복사본 폴더를 로컬에 생성.

로컬에 생성된 레포지토리 폴더 오른쪽 클릭 후 git bash로 열기

git push origin master //깃허브의 레포지토리를 최신버전으로 업데이트

새로 업로드할 파일을 로컬의 깃허브 레포지토리 폴더에 넣는다

git add * //현재 폴더의 모든 파일을 추가

git commit -m "명령어"
git push origin master